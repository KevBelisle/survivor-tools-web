# Default API URL for production builds (fallback only)
# The actual API URL should be set at Docker runtime via the API_BASE_URL environment variable
# See inject-env.sh for runtime injection details
VITE_API_BASE_URL=https://api.survivor.tools
